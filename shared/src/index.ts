/**
 * WhereWeWere check-in type plugin contract.
 *
 * Shared by the client and server workspaces. A plugin describes a new
 * check-in type: the shape of its stored data, its strings, and (optionally)
 * its UI, filters, profile tab, settings sections, and storage.
 */

export * from './fields';
export * from './timeline';
export * from './plugin';
